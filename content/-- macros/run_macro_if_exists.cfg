[gcode_macro _run_macro_if_exists]
gcode:
    {% if printer["gcode_macro %s" % params.MACRO] != null %}
        { "%s " % params.MACRO }{% for p in params %}{ "%s=%s " % (p,params[p]) }{% endfor %}
    {% endif %}
